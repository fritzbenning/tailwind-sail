export const DEFAULT_PX = 1;

/** Default-theme numeric `ring` / `inset-ring` width (px). */
export const WIDTH_PX: Readonly<Record<string, number>> = {
	"0": 0,
	"1": 1,
	"2": 2,
	"3": 3,
	"4": 4,
	"8": 8,
};
